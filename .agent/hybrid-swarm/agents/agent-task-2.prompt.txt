# Task: Integrate pathfinding into merchant navigation

Update merchant navigation to use the new A* pathfinding instead of pure gradient-following. Changes needed:

1. **src/agents/sensory.rs** — In find_nearest_city(), filter candidates to only cities reachable via terrain.is_reachable(). If no reachable city exists, fall back to the current nearest-Euclidean behavior. Add a find_path_to_city() method that returns waypoints from terrain.find_path().

2. **src/agents/merchant.rs** — Add a waypoint queue (Vec<(u32,u32)> or VecDeque) field to the Merchant struct. When a merchant selects a new destination city, compute the A* path and store waypoints. During tick_move(), steer toward the next waypoint instead of directly toward the city. Pop waypoints when within ~1.5 tiles. Recompute path if merchant gets stuck (same position for N ticks) or destination changes.

3. **src/brain/trader.rs** — Update the steering logic: when waypoints are available, compute heading toward the next waypoint rather than using the gradient-based city heading. Keep terrain_avoidance() as a secondary safety layer but the primary navigation should follow waypoints. The existing collision resolution in resolve_terrain_collision() should remain as a last-resort fallback.

Target files: src/agents/sensory.rs, src/agents/merchant.rs, src/brain/trader.rs
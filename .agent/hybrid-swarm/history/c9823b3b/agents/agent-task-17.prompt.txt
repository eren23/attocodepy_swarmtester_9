# Task: Final integration review and validation

Review the complete codebase for correctness, consistency, and completeness against the spec. Verify: (1) All 22 commodity types are handled in market engine, crafting, and rendering. (2) All 7 profession FSMs have correct state transitions and trait modulation. (3) The tick update order in World::tick() is correct and avoids stale data. (4) All config parameters from economy_config.toml are actually used (no dead config). (5) Mountain/water impassability is enforced everywhere (movement, city placement, resource placement). (6) Seasonal effects apply correctly to all systems (resources, speed, food consumption, harbors, bandits). (7) Gold conservation: total system gold is constant (modulo NPC treasury injection). (8) All 15 emergent behavior tests have correct statistical methodology. (9) Performance targets are realistic for the data structures chosen. (10) The rendering correctly reflects all simulation state. (11) All keyboard controls from spec are implemented. (12) Headless mode produces valid JSON matching spec schema. (13) README.md covers all required sections. Flag any spec violations, missing features, or architectural concerns.